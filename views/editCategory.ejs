<!-- editCategory.ejs -->
<html>
  <head>
    <link rel="stylesheet" href="/styles.css">
    <link rel="stylesheet" href="/global.css">
    <link rel="stylesheet" href="/newOrEditCategory.css">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Edit Category</title>
  </head>
  <body>
    <%- include("partials/header.ejs") %>
    <div class="container">
        <div class="heading">
            <h2>Edit a Category</h2>
        </div>
        <div class="main">
            <form class="categoryForm" action="/categories/edit/<%= category.id %>" method="post">
                <!-- Category name -->
                <label>
                    Category name
                    <input type="text" name="name" placeholder="Fruits" value="<%= values?.name || category.name %>" required>
                </label>

                <!-- Category color -->
                <label class="colorPicker">
                    Color
                    <div class="colorInput">
                        <input type="color" name="color" value="<%= values?.color || category.color %>" required>
                    </div>
                </label>

                <% if (errors && errors.length > 0) { %>
                <ul class="errors">
                    <% errors.forEach(err => { %>
                    <li><%= err.msg %></li>
                    <% }) %>
                </ul>
                <% } %>

                <button type="submit">Edit Category</button>
            </form>
        </div>
    </div>
    <%- include("partials/footer.ejs") %>
  </body>
</html>
